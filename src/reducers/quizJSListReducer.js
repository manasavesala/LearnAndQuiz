import constants from './../constants';
const { initialState, types } = constants;

const quizJSListReducer = (state = initialState.quizJSList) => {

    return state;

};

export default quizJSListReducer;